# Frontend Shared

This directory is reserved for browser-safe shared code.

- Put frontend UI primitives, API helpers, media helpers, and browser-safe utilities here.
- Do not import Express, Prisma, or backend-only feature code here.
- Cross-runtime, framework-agnostic primitives belong in the repo-level `shared/` directory instead.
