# Comments Module

Owns media comments, public and friend feeds, reactions, and external comment ingestion.

Provider discovery and external comment fetching live behind module infrastructure adapters.

HTTP adapters remain compatible with the existing `/api/comments` and `/api/external-comments` routes.
