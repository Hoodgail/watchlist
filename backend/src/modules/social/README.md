# Social Module

Owns follow relationships, public profile visibility, and suggestion workflows.

HTTP adapters remain compatible with the existing `/api/friends`, `/api/profile`, and `/api/suggestions` routes.
