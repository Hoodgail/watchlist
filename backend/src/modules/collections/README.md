# Collections Module

Owns collection lifecycle, permissions, membership, items, invites, stars, and collection-local comments.

HTTP adapters preserve the existing `/api/collections` surface while collection rules now live behind application ports, domain policies, and infrastructure gateways.
