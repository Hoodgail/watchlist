# Backend Shared

This directory is reserved for server-only shared code.

- Put backend ports, server-side shared DTOs, Prisma mappers, and server-safe utilities here.
- Do not put React, browser, or frontend feature code here.
- Cross-runtime primitives that are safe for both backend and frontend belong in the repo-level `shared/` directory instead.
